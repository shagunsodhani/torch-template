---
# @package _group_
builder:
  cls: codes.dataset.cifar10.get_dataloaders
params:
  # name: cifar10
  # cls: codes.dataset.${dataset.name}
  dir: ${general.base_path}/data
  dataloader:
    train:
      batch_size: 128
      shuffle: true
      num_workers: 2
    test:
      batch_size: 128
      shuffle: false
      num_workers: 2
